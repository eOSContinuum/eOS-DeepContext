[project]
name = "eos-deepcontext-site"
version = "0.1.0"
description = "Static site generator for the eOS-DeepContext knowledge graph"
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.6",
]

[tool.uv]
managed = true
